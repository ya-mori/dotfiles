# private/ という名前のディレクトリは、階層を問わず git 管理しない。
#
# 秘匿情報（識別子・社内固有名詞・実名・社内制度）の置き場。
# public 側と同名のファイルを置き、差分だけを持たせる規約。
# 詳細は README「公開方針（L1/L2/L3）」を参照。
#
# 注意: chezmoi の `private_` 属性 prefix（パーミッション 0600）とは別物。
# あちらは underscore 付きのファイル名なので、このパターンにはマッチしない。
**/private/
