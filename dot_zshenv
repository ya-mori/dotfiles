# すべての zsh（非対話・GUI 起動含む）で読まれる環境設定

# 秘匿トークンは macOS Keychain を単一ソースとして注入する
export GITHUB_PERSONAL_ACCESS_TOKEN="$(security find-generic-password -s github -a "$USER" -w 2>/dev/null)"

# マシン固有の追加シークレットは管理外ファイルへ
[ -f "$HOME/.zshenv.local" ] && source "$HOME/.zshenv.local"
